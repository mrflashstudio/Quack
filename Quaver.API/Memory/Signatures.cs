namespace Quaver.API.Memory
{
    public static class Signatures
    {
        public static readonly Signature QuaverBase = new Signature
        {
            Pattern = "48 BA ?? ?? ?? ?? ?? ?? ?? ?? 48 8B 12 48 8B 92 ?? ?? ?? ?? 4C 8B 81 ?? ?? ?? ?? 4C 8D 89",
            Offset = 0x2
        };
    }
}
